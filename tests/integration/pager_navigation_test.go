package integration_test

import "testing"

// This test is a placeholder for interactive pager navigation validation.
// Automated execution requires a Windows runner and a pseudo-tty. For now,
// the test is skipped and serves as documentation for manual verification.
func TestPagerNavigationManual(t *testing.T) {
	t.Skip("manual integration test: run on a Windows runner with a TTY to validate interactive pager")
}
