package integration_test

import "testing"

func TestTUIConsistencyManual(t *testing.T) {
	t.Skip("manual integration test: verify list and status share the same navigation model in an interactive terminal")
}
